// - Node run on a serveur

// - The console is terminal window
console.log("Hello World");

// - Global object instead of window object
//console.log(global);

// - Common Core module (ex)
//const os = require('os');

//console.log(os);

// - CommonJS modules insterd of ES module 
//const math = require('./math');

//console.log(math.add(2, 3));

// - Missing some JS APIs like Fetch